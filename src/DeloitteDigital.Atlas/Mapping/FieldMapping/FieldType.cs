namespace DeloitteDigital.Atlas.Mapping.FieldMapping
{
    public enum FieldType
    {
        // For field mapping a single value fields to a single object
        Single,

        // For field mapping multi value fields (ie Multilist) to a collection of objects.
        Multi
    }
}